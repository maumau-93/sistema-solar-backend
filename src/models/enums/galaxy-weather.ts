/**
 * Galaxy Weather enum indicates the current condition of the weather
 * for the given galaxy.
 */
enum GalaxyWeather {
  Drought = 'drought',
  Rainy = 'rainy',
  OptimalContitions = 'optimal',
  Unkwnown = 'unknown'
}

export default GalaxyWeather;
