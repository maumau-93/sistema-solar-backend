/**
 * Planet Direction enum indicates whether the planet is going through its orbit
 * in clock-wise or counter clock-wise direction
 */
enum PlanetDirection {
  Clockwise = 1,
  CounterClockWise = -1
}

export default PlanetDirection;
